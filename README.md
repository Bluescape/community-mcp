# Bluescape MCP Demo

This project is a Model Context Protocol (MCP) server for creating and managing
editable content in Bluescape workspaces from an MCP client such as typical LLMs
used conversationally by human users.